overwrite: true
schema: "schema.graphql"
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript-type-graphql"
  ./graphql.schema.json:
    plugins:
      - "introspection"
